create table users(
username varchar primary key,
password varchar,
name varchar, 
email varchar, 
phone varchar
);

create table admin(
username varchar primary key,
password varchar,
name varchar, 
email varchar, 
phone varchar
);